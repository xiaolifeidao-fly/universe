# Leave empty when the PWA is served behind the app-api reverse proxy.
NEXT_PUBLIC_APP_API_BASE_URL=http://localhost:10002

# Enables visual foundation records only for local UI review. Never set in deployment.
NEXT_PUBLIC_APP_PREVIEW=false
