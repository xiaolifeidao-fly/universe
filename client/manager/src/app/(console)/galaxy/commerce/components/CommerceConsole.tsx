"use client";

import { ModelCatalog } from "../../components/ModelCatalog";

/**
 * 商品与定价。
 *
 * 原先是三个页签：模型、单价、额度包。现在只剩模型一张表 ——
 *
 *   · **额度包已经下架**。计费改成按单价逐笔扣账户余额之后，「先买一包额度再慢慢用」
 *     这层中间物不再有对应的东西可卖：使用端不下单、运营也不维护商品，
 *     那个组件连同它的定价算式一并删了。历史订单仍然在「订单」页上查得到。
 *
 *   · **单价搬进模型行**。价目表是 `能力 × 模型 × 计量单位 × 生效时间` 四维展开的，
 *     摊平成一张表就是几十上百行，而运营心里想的是「把 sonnet 的价改一下」——
 *     那件事原先要在一张表里先筛能力、再认出四个单位各一行。现在点模型行上的
 *     「定价」，一屏里就是这个模型的输入 / 输出 / 缓存，各自两个价。
 *
 * 搬家带来两件**不属于任何模型**的东西，它们跟着落在模型页顶上，不能一起收走：
 * 兜底价（modelId 为空的那些行，所有没单独定价的模型都按它算）和「有量无价」告警。
 * 查不到价时扣费与结算是**静默算 0**，不报错 —— 少了这两个入口，故障只能从
 * 「钱一直对不上」反推。
 */
export function CommerceConsole() {
  return <ModelCatalog />;
}
