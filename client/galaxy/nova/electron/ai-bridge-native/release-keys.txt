# ai-bridge 发布签名公钥：远程升级与 `ai-bridge upgrade` 的信任根。
#
# 编译期由 src/pool/upgrade.rs 用 include_str! 嵌进可执行文件。节点只安装用这里某一把
# 公钥对应的私钥签过名的安装包 —— Hub 下发的下载地址和 sha256 都不单独可信，
# 平台被人拿下了，也推不了一个没签过名的包到别人的机器上。
#
# 格式：一行一把，32 字节 Ed25519 原始公钥的标准 base64（44 个字符）。
#       `#` 之后是注释，空行忽略。
#
# **一把都没有时不能远程升级**：节点在 hello 里报 upgradeBlocker，控制台上的升级按钮不亮。
#
# 生成（一次就够，私钥离线保管，不进仓库）：
#   node scripts/release-sign.cjs keygen
# 它打印的公钥同时加到两处，缺一处都不行：
#   1. 这个文件（然后重新编译发布包）；
#   2. manager-api 配置 galaxy.bridge_release.public_keys（逗号分隔），管理后台上传时先验一遍。
#
# 换钥匙：先把新公钥加进来、仍用旧私钥签发一版（新旧两把并存）；等机器都升到这一版，
# 再改用新私钥签、删掉旧公钥。一步到位的话，还在跑老版本的机器只认旧钥匙，
# 验不过新包，只能挨台手动重装。

EgQ3poF7af3/o2VbOjErGu995fpElPD2ue18ShlRBqM=  # 2026-09-21 生成，私钥离线保管
