# 进程监听地址。main.go 和 start.sh 都读这一项，改端口只改这里。
# （server.port 是旧配置，当前没有任何代码在读，保留只为兼容外部脚本）
server.address=:8691
request.path=/api

# sqlconn = qa_conn:qa_conn@tcp(192.168.5.12:3333)/universe?charset=utf8&parseTime=True&loc=Local

sqlconn = tbk_2026:tbkA4622511z3!@tcp(rm-1udlgbce4t64wmpdl9o.mysql.rds.aliyuncs.com:3306)/universe?charset=utf8&parseTime=True&loc=Local

log.dir=./logs/

# redis.addr = r-1udr0cqum8k2jguku3pd.redis.rds.aliyuncs.com:6379
# redis.password = tbkA4622511z3!
# redis.mode = standalone

redis.addr = 172.16.49.81:6385
redis.password =
redis.mode = cluster

auth.default_username = admin
auth.default_display_name = Administrator
auth.default_password = admin123
auth.token_ttl_seconds = 604800
# Must be replaced per environment before production deployment.
auth.token_secret = change-this-development-token-secret
auth.service_token = local-service-token


# Chat simulation AI service (primary AI provider).
# base_url: e.g. http://your-ai-service:8080
chat.simulation.base_url = http://indo-chatter.x-amc.wke-office.test.wacai.info
chat.simulation.flow_code = FLOW_001
chat.simulation.timeout_ms = 30000

# Optional AI reply provider for WhatsApp consumer (fallback if chat.simulation.base_url is empty).
whatsapp.ai.endpoint =
whatsapp.ai.api_key =
whatsapp.ai.model =
whatsapp.ai.system_prompt = 你是 WhatsApp 客服助手，请用简洁、自然、专业的语气回复用户。
whatsapp.ai.timeout_ms = 8000

ark.url.pre = http://indoark-web-api.x-amc.wke-office.test.wacai.info/indoark-web-api
case.detail.token =
case.detail.timeout_ms = 8000
# 联调开关：true 时 /cases/detail 直接返回 mock 数据，不请求 ark。正式接口就绪后置为 false。
case.detail.mock = true

# ---- 外部大模型（第 5 层唯一的按量付费依赖） ----
# api_key 留空即全系统走规则模式：归因照常出报告，标记 fallback=true。
llm.api_key =
# 留空则用默认值 https://api.anthropic.com 与 claude-opus-5
llm.base_url =
llm.model =
