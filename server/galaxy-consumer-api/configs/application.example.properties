# galaxy-consumer-api：Orbit 使用端控制台与账号（/api/galaxy/consumer/*）、
# 门户（/api/galaxy/portal/*，**未登录**就能打）。
#
# 它管的是「看」和「用」：自助签发与取回算力密钥、余额与消费流水、用量与账单、门户展示。
# **这一端不卖任何东西**：额度就是账户里的积分余额，只能由运营在管理端充进来。
# 派单、节点协议、SDK 流式请求全在 galaxy-hub-api；提供端控制台在 galaxy-api。
# 文件末尾列了「故意不在这里」的清单，别再从别的服务抄回来。
#
# 进程监听地址。main.go 与 start.sh 都读这一项，改端口只改这里（GALAXY_CONSUMER_API_ADDR 优先于它）。
server.address=:10005

# ---------- 优雅退出 ----------
#
# 收到 SIGTERM 之后的顺序是定死的：翻 readyz→503 并停止领新活 → 等 drain_delay
# 让上游把这台摘出轮转 → 关监听、等在途请求跑完（上限 timeout）→ 退出。
#
# drain_delay 是给「摘流量不是瞬时的」留的余量：nginx reload、K8s 更新 Endpoints
# 都要一会儿。这一段不等就关监听，那几百毫秒里进来的请求吃的是连接拒绝 ——
# 明明是计划内的重启，用户看到的却是故障。
# galaxy.shutdown.drain_delay_ms = 5000
#
# timeout 是等在途请求跑完的上限。控制台没有长连接，请求都是秒级的，30 秒够了。
# galaxy.shutdown.timeout_ms = 30000
#
# 部署侧要跟着配：systemd 的 TimeoutStopSec、K8s 的 terminationGracePeriodSeconds
# 都必须大于这两项之和，否则进程会在收尾到一半时被补一刀 SIGKILL。

# ========== 公共段：三个服务必须填同一份 ==========

sqlconn = user:password@tcp(127.0.0.1:3306)/universe?charset=utf8&parseTime=True&loc=Local

# 控制面。没有 Redis 不能启动 —— 座位与额度的原子性靠它，控制台读到的额度也来自这里。
redis.addr = 127.0.0.1:6379
redis.password =
redis.mode = standalone
galaxy.redis_namespace = galaxy

# 启用哪些能力契约。门户目录与套餐按它列出能买到什么，Hub 按它装配真正的传输适配器；
# 两边写得不一样就会出现「门户在卖、Hub 派不出去」。只填 relay 就是一个纯中转站部署。
galaxy.adapters = relay,delivery,videofarm

# ========== 使用端控制台 ==========

# 控制台账号是 Galaxy 自己的（共享端 / 使用端两批人），和任务宇宙（web-api / app-api）
# 的账号体系无关，令牌不通用。签名密钥单独配一个，别和那几个服务的 auth.token_secret 抄成同一个。
# 没配 galaxy.auth.* 时退回 auth.token_secret / auth.token_ttl_seconds（老部署不改配置也能起来）。
galaxy.auth.token_secret = change-this-galaxy-token-secret
galaxy.auth.token_ttl_seconds = 604800

# 算力密钥明文的加密密钥。库里存的是用它加密过的明文：使用端「一键使用」、运营在管理端
# 查看并转交密钥，都要解得开。**manager-api 和 galaxy-hub-api 必须配同一个值** ——
# Hub 的 /v1 换发也是用它封的，三边不一致就会出现「有的密钥取得回、有的取不回」。
# 不配也能跑：密钥照常签发和鉴权，只是新签的都取不回明文。配上之后就别再改 ——
# 改了之前签的全部解不开（鉴权不受影响，按哈希查），只能让使用者换发。
galaxy.key_cipher_secret = change-this-key-cipher-secret

# 消费者 SDK 要填的 base_url（消费者打的是 Hub 的 /v1/*，不是这个服务）。
# 控制台的「一键使用」、取回密钥的弹窗、门户首页的接入地址，展示的都是这一条原样的字符串，
# 所以它必须穿得过公网入口。不配就从 galaxy.instance 派生，而这个服务根本没有 instance
# 语义，派生出来的是一个用户够不到的本机地址 —— 生产部署必须显式写。
# 线上是 https://www.galaxy.rodeo/hub-api/v1，前缀由 nginx 剥掉（同 galaxy.provider_hub_url，
# 详见 doc/deployment/galaxy-services.md「三个『对外地址』」一节）。
galaxy.consumer_base_url = http://127.0.0.1:10006/v1

# 使用端桌面客户端（Orbit）的下载地址。使用端控制台的密钥页会把它原样摆出来。
# 控制台自己就是这个客户端的界面，但它同时挂在浏览器上；而把密钥一键写进本机
# Claude Code / Codex 配置的那个「使用」按钮只有桌面壳里才有 —— 浏览器里的人
# 需要一条路把客户端拿到手，就是这一条。
# 填什么由部署方定：直指某一个安装包，或者指一个列出各系统安装包的下载页（mac 是 dmg、
# Windows 是 exe、Linux 是 AppImage，一条地址盖不全三个，指一个页更省事）。
# 不配就是那一块不显示 —— 安装包托管在哪儿服务端猜不出来，猜一个只会换来一次 404。
#
# 这一条现在是**默认值**：管理端「ai-bridge 版本」页顶上那张卡片改的是数据库里的
# client.consumer_download_url，它盖过这里。换个地方放安装包不必再动这个文件、重启进程；
# 卡片上点「清除」就退回这里写的值。共享端 Nova 的下载地址只在那张卡片上，没有对应的配置项。
# galaxy.consumer_client_download_url = https://www.galaxy.rodeo/download

# 对外声明的模型清单：控制台的模型下拉、门户的模型卡片都按它渲染，
# 要和 Hub 那份（GET /v1/models 返回的）写成同一份，否则页面上能选的模型 SDK 拿不到。
# 写的顺序就是下拉里的顺序。不配走代码里的兜底清单。
# 它只是一份声明，和派单无关：能不能真派出去仍看各节点自己的允许/拒绝规则，
# 多写一个没人提供的模型，换来的是一次 no_capacity。
# galaxy.models = claude-opus-5,claude-sonnet-5,claude-fable-5-1,claude-haiku-4-5-20251001,claude-opus-4-8,claude-opus-4-7,claude-opus-4-6,claude-opus-4-5-20251101,gpt-5.6-terra,gpt-5.6-sol,gpt-5.6-luna

# 以下留空即用设计文档第 15 节的默认参数。
#
# 新签发密钥的有效期与到期后的冻结期（冻结期内还能换发，过了就真没了）。
# Hub 的 /v1 换发按同一套参数算，两边要一致。
# galaxy.key_ttl_days = 30
# galaxy.key_freeze_days = 30
# 数据告知版本。升版后旧的确认失效，下次签发新密钥时要求重新确认 —— 和 Hub 填同一个值。
# galaxy.consumer_notice_version = consumer-notice/v1

# 门户面（未登录可打）。
#
# availability 是**部署方的承诺**，不是算出来的指标。不配就是空串，门户首页
# 那排数字少显示一格 —— 默认给个 99.9% 等于替你许了一个你没许的诺。
# galaxy.portal.availability = 99.9%
#
# 只读接口的缓存时长。公开地址直通数据库就是一条没有闸门的压测通道。
# galaxy.portal.cache_ttl_ms = 30000
#
# 整套部署每小时最多收多少条「联系我们」。它是「有人换着 IP 刷」的兜底闸；
# 按 IP 的那道（一小时 5 条）在库里，跨实例一致。
# galaxy.portal.leads_per_hour = 60

# 交付类产物（delivery / videofarm）的下载地址在这里签。不配则产物列表点开是空的。
# 上传那一侧在 Hub，两边配同一个桶。
oss.enabled = false
oss.dirPrefix = galaxy
oss.endpoint =
oss.bucketName =
oss.accessKeyId =
oss.accessKeySecret =
oss.expireTime = 600
oss.tokenExpireTime = 300

# ========== 故意不在这里的配置 ==========
#
# 下面这些键这个进程一个都不读，抄进来只会让人以为改了有用：
#
#   galaxy.instance                 只有 Hub 有实例语义（派单时写进 streamURL）
#   galaxy.provider_hub_url         给节点看的地址，见 galaxy-api / galaxy-hub-api
#   galaxy.contract_version         节点 hello 时校验，只有 Hub 收 hello
#   galaxy.provider_terms_version   提供者条款，见 galaxy-api / galaxy-hub-api
#   galaxy.payout_* / referral.*    提供端的收益与返现，见 galaxy-api
#   galaxy.payment.*                额度包与支付已经下架，这几个键谁都不读了
#   galaxy.audit.* / bridge_release 抽检与安装包分发都在 Hub
#   派单参数（platform_seat_limit / bind_idle_ttl / heartbeat_timeout / spill_wait /
#   max_wait / attach_timeout / node_probe / stream_idle / body_limit / journal_buffer）
